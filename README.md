# 深潜 · SINK

一盏会发光的灯，向深渊下潜。**越贪越快，越快越瞎。**

- **深度 = 分数**，氧气是唯一生命条，归零即结束
- **加速下潜**：得分 **×2.5**，但氧气消耗 **×2.05**、视野同时收缩
- 气泡回氧、发光水母临时扩光、礁石与废弃缆线要掉氧，**擦身而过 +5** 分
- **气泡越深越稀**（间隔 ×(1 + 深度/700 × 0.9)）：深水靠捡泡续命是可行的，但**不再是净回氧的永动机**
  （原来这个系数是 4000，脚本对局 3/9 种子会跑成 300s 不朽 —— 有氧上限形同虚设）
- 越深越黑：可视半径从 360px 收到 150px
- **光照半径之外是纯黑** —— 障碍轮廓、气泡一律看不见；只有**水母是光源**，隔着黑也看得见它游过来
- 捡一只水母：视野 **+200px**，约 20 秒才衰减干净（深渊馈赠的「流萤」每级再 +60）

## 深渊压力（随深度加压）

深度会被折算成 **0→100% 的压力值**（右上角实时显示），压力同时拉动四件事：

```
压力 = 深度 / (深度 + 1500)        // 600m→29% · 1500m→50% · 4500m→75%，永远到不了 100%
```

| 深度 | 压力 | 耗氧 | 气泡含氧 | 撞击伤害 | 障碍行间隔 | 走廊宽 | 多一个障碍的概率 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 0 m | 0% | ×1.00 | ×1.00 | 15 | ×1.00 | ×1.00 | 0% |
| 600 m | 29% | ×1.14 | ×0.94 | 17 | ×0.96 | ×0.98 | 9% |
| 1500 m | 50% | ×1.25 | ×0.90 | 18 | ×0.93 | ×0.96 | 15% |
| 4500 m | 75% | ×1.38 | ×0.85 | 20 | ×0.90 | ×0.94 | 23% |

曲线是**对数式**的：前 600 米就能感觉到，之后增长越来越慢但**永不封顶**（9000m 也只到 86%），
所以它可以继续往上叠新机制而不会失控。

## 深渊馈赠（割草式三选一）

下潜到阈值就**暂停**并弹出三张卡，点一张（或按 `1` `2` `3`）拿走效果，然后继续下潜：

```
升到第 n+1 级所需深度 = 100 + 80 × (n-1)      // 100 / 280 / 540 / 880 / 1300 / 1800 …
```

16 张牌，各 5 级，效果**线性叠加**（`节氧` `耐压` 是乘算，收益递减）：

| 类型 | 卡 | 每级效果 | 解锁 | 权重 |
|---|---|---|---|---|
| 收益 | 贪婪 | 加速得分倍率 +0.30 | 0 m | 1.0 |
| 收益 | 打捞 | 气泡含氧 +4 | 0 m | 1.0 |
| 收益 | 擦身 | 擦身 +6 分 · 判定垫 +4px | 120 m | 1.0 |
| 生存 | 气密 | 氧气上限 +18（立刻补上） | 0 m | 1.0 |
| 生存 | 节氧 | 耗氧 ×0.90 | 200 m | 0.9 |
| 生存 | 耐压 | 撞击伤害 ×0.85 | 200 m | 0.9 |
| 生存 | 缓冲 | 受击无敌 +0.30s | 400 m | 0.8 |
| 机动 | 流线 | 常速 +8% | 0 m | 1.0 |
| 机动 | 舵效 | 转向响应 +22% | 0 m | 1.0 |
| 机动 | 推进 | 加速速度 +8% | 120 m | 1.0 |
| 采集 | 磁力 | 吸附半径 +55px | 150 m | 0.95 |
| 采集 | 流萤 | 水母视野 +60 · 更常见 | 150 m | 0.95 |
| 战斗 | 火力 | 普攻伤害 +25 | 170 m | **2.2** |
| 战斗 | 速射 | 普攻间隔 ×0.82 | 240 m | **2.2** |
| 战斗 | 碎岩 | 普攻对障碍物 +50 伤害（**Lv1 起石头一发碎**） | 330 m | **2.2** |
| 战斗 | 散射 | 普攻光束 +16px 宽 | 420 m | **2.2** |

**战斗牌有保底**：战斗牌总等级 < 2 时，每次**普通**三选一必带一张战斗牌（选路线那次是专门的三张路线，不掺别的）。保底不受深度门槛限制——池里没有就把最早解锁的那张补进来，否则前两次强化会空转（实测第一次拿到战斗牌会被推到 540m）。
保底退场后靠权重，占比仍有 ~84%。实测（400 次/深度）：

| 深度 | 保底生效时 | 保底退场后 | 旧版 |
|---:|---:|---:|---:|
| 300 m | **100%**（战斗牌 2/13） | — | 26.5% |
| 700 m | **100%**（战斗牌 4/16） | **84%** | 61.8% |

实战一趟 3000m（AI 只想拿战斗牌）：第一次拿到战斗牌从 **540m 提前到 100m**（第一次强化就拿到）；保底生效期间普通三选一 400 次**全带**战斗牌（`test/verify.mjs` 实测）。

抽卡用**独立随机流**（由 `G.seed` 与 `G.level` 派生），所以同一 seed 的障碍布局不会因为抽卡而改变 —— 截图与复盘可复现。
所有强化在 `recalcMods()` 里汇总成 `G.m` 一张系数表，玩法只读 `G.m`；**加新卡 = 往 `SKILLS` 里加一行 + 在 `G.m` 里接一根线**。

按「带强化 / 无强化」同种子对拍（9 种子）：中位 **74.4s vs 71.5s**、最长 **109.6s vs 89.1s**。
即：强化**不破坏 60~120 秒的时长带**，但把天花板抬高了 —— 强化后的深度能到 1600m+，留给后续机制继续叠。

加上战斗系统后再标定（9 种子，脚本 AI 会瞄准开火）：中位 **87.6s**、最短 72.2s、最长 125.3s，**8/9 在 60~120s 带内**。
（同为 9 种子的旧版是 74~80s 中位，战斗把分布**收窄**了：能打掉挡路的障碍，但 1000m 以下开始有东西还手。）

单文件、零依赖、**零素材**（美术全程序化，音效用 WebAudio 现场合成）、零外部网络请求。

## 战斗（准星 / 普攻 / 深海猎食者）

**「在哪」和「打哪」分成两只手**：`A` `D`（或 `←` `→`、触屏拖拽）推**本体**；**鼠标就是准星**，普攻从潜艇射向准星落点。

- **普攻是 hitscan 光束**，判定宽度 = `atkHalfW`（半宽 22px），**自动连发**（间隔 `atkEvery` 0.5s），不需要按键
- 光束**同时对障碍物和怪物造成伤害**，一发打穿路径上所有命中的东西
- 初始伤害 **50**：**石头 100 血 → 两发碎**，**泥鳅 30 血 → 一发碎**；碎掉的障碍物 +6 分，怪物 +40 分
- **自动瞄准兜底**：鼠标停手 2.5s（`aimIdle`）或触屏/键盘玩家，准星自动咬住最近的怪物；一动鼠标立刻收回控制权
- 准星、光束、血条、怪物**都画在黑暗层之上** —— 光是打出去的，不该被黑暗吃掉

**深海猎食者**（`mobFrom` **1000m** 以下才出现，浅水区照旧只有地形）：

| 怪物 | 血量 | 行为 | 还手方式 |
|---|---:|---|---|
| 猎手（红梭） | 150 | 加速上冲 + 横向咬人（约 1.3s 穿屏） | 撞上扣 **18 氧**，同归于尽 |
| 电袭者（紫伞） | 90 | **悬停射手**：不跟水流滚，自己慢慢游上来 | 每 1.3~2.0s 放一颗电球，命中扣 **12 氧** |

越深来得越勤，但**刚过线有一段适应期**（`rate = (0.30 + 0.70×爬升) × (0.70 + 1.40×压力)`，
爬升 = 离 1000m 的距离 / 400m —— 即**只有 1000~1400m 这一段被软化，1400m 起完全还原原版压力**），
而且**同时在场有硬上限**（`2 + 4×压力`，即 4~5 只）。
脚本 AI 定深实测（40s，1010m 那行是 20s）：

| 深度 | 累计刷出（旧） | 累计刷出（新） | 同时在场峰值 |
|---:|---:|---:|---:|
| 1010 m | 15 | **4** | 1 |
| 1100 m | 31 | **14** | 2 |
| 1500 m | 33 | **34** | 3 |
| 2500 m | 37 | **37** | 3 |

**为什么要专门测深段**：怪是氧气压力的主承重墙。第一版我把 1000m 以上的密度整体砍到 1/5，
结果脚本对局中位从 81s 飙到 183s、3 局撞满 300s 上限 —— 难度带整条崩掉。
所以浅段软化后，深段必须还原（上表最后一列就是这条的回归判据）。

> 顺带修掉一个被我这次改动**翻出来**的旧漏洞：`bubbleThin` 4000 太松，深水「完美捡泡 = 净回氧」，
> 有氧上限形同虚设（对照实验：密度完全还原时 9 个种子里仍有 3 个跑成 300s 不朽）。
> 改成 **700** 后中位 87.6s、**8/9 落在 60~120s 带内**。

## 转向手感（A/D）

键盘走的是**油门**，不是开关：轻点微调，按住提速到全速。

| | 旧 | 新 |
|---|---:|---:|
| 轻点 0.16s 的峰值速度 | 620（97% 全速） | **194.6（30%）** |
| 轻点一次位移 | 106.6 px | **32.3 px** |
| 松手后漂移 | 26.3 px | **8.1 px** |
| 到达 90% 全速用时 | 0.083s | 0.417s |
| 贴边 20px 推墙的撞击速度 | 461.4 | **0.9**（软着陆） |

曲线 = `kbTapMul 0.30 → 1.0`，`kbHold 0.12s` 之后按 `kbRamp 0.35s` 走 smoothstep；
贴边 `kbSoftEdge 120px` 内线性收力，不再撞墙急停。鼠标转向仍是**比例控制**，不受影响。

## 调参旋钮

想改难度/手感都在这几个数上（`CFG`，改完刷新即可）：

| 旋钮 | 作用 |
|---|---|
| `diffHalf` | 深渊压力曲线快慢（越大越温和） |
| `mobFrom` / `mobRamp` / `mobIntro` | 刷怪起始深度 / 适应期长度 / 适应期倍率 |
| `mobCapBase` / `mobCapGain` | 同时在场上限 |
| `mobDeepen` | 深段倍率（`[0.70, 1.40]` ＝原版，**别动**：砍了局时会失控） |
| `hunterEvery` / `spitterEvery` | 两只怪的基础间隔 |
| `bubbleThin` | 气泡随深度变稀的速度（越小越难） |
| `kbTapMul` / `kbHold` / `kbRamp` | 键盘轻点档位 / 生效延迟 / 提速时长 |
| `kbSoftEdge` | 贴边缓冲宽度 |

## 怎么玩

| 操作 | 键位 |
|---|---|
| 转向（本体） | `A` `D` / `←` `→` / 触屏拖动 —— **轻点微调，按住全速** |
| 瞄准（准星） | **鼠标移动**（触屏/键盘由自动瞄准兜底） |
| 加速下潜 | 按住鼠标左键 / `空格` / 触屏**双指** |
| 普攻 | **自动连发**，打准星落点（不需要按键） |
| 选强化 | 点卡片 / `1` `2` `3` |
| 暂停 | `P` / `Esc` |
| 重开 / 回标题 | `R` / 结算页 `Esc` |
| 静音 | `M` |

**直接双击 `index.html` 就能玩。** 想用本地服务（推荐，localStorage 在 `file://` 下部分浏览器会受限）：

```bash
cd D:/DSH/games/deep-dive
python -m http.server 8080      # 或 npx serve .
# 打开 http://127.0.0.1:8080/
```

手机同局域网访问：`http://<本机IP>:8080/` —— 建议**横屏**。

## 发到 itch.io

1. 新建项目 → Kind of project 选 **HTML**
2. 把 `index.html`（单文件即可）打包成 zip 上传；勾选 *This file will be played in the browser*
3. 建议视口：**960 × 600**（或勾选 “Mobile friendly” 后由页面自适应；本作竖屏会留黑边，已在画面里提示横屏）

## 发布到 GitHub Pages

```bash
node tools/align-head.mjs      # 必跑：delivery_check 的 encoding-utf8 只取头 64KB 做严格解码，
                               # 改完 index.html 后 64KB 切线可能正好落在汉字中间 → 门禁判 FAIL
node tools/publish-pages.mjs   # 走 Contents API 覆盖 index.html / README.md / .nojekyll
```

线上地址：<https://ybbcmg.github.io/sink/>（发布后轮询比对 md5sum，确认 CDN 真的换新了再说“完成”）。

## 自检与验收

页面内置自检屏，直接在地址后加参数：

| 参数 | 作用 |
|---|---|
| `?selftest=1` | 跑 7 个固定种子的完整对局，在屏幕上打印时长/深度/判定 |
| `?scenario=over` | 直接跳到结算页（自动打到死） |
| `?scenario=deep&depth=1200` | 直接跳到指定深度的实机画面（截图/看光照用） |
| `?scenario=levelup` | 停在第一张「三选一」卡面上 |
| `?scenario=build&depth=900` | 自动选牌跑到指定深度，看一身强化的 HUD |
| `?scenario=combat&depth=1200` | 把猎手/电袭者/半血石头/泥鳅/电球摆进视野，开一枪后定格（战斗截图） |
| `?auto=N&seed=K` | 跑 N 个固定步长（1/120s）后定格，用于可复现截图 |

无头浏览器验收（生成 `shots/*.png` + 一份判定 JSON）：

```bash
node test/verify.mjs
```

覆盖：语法可解析 / 零 pageerror / 零 console error / 零外部请求 / 十张截图 / 局时 / 键盘·鼠标·触屏三套输入 /
**空格+鼠标微动不掉速（回归）** / **深渊馈赠三选一链路（开卡·冻结·落账·门槛·不重复·满级不泄漏）** /
**黑暗层（半径外连气泡都看不见、水母可见、水母光强与续航、全画布零漏光）** /
**战斗（血量设定·石头两发碎·泥鳅一发碎·光束够不着不掉血·准星跟鼠标·打哪由准星决定·1000m 以下才刷怪·电球与猎手真的扣氧·四张战斗牌改系数·碎岩一发碎·自动连发）** /
**转向手感（轻点=微调·按住=全速·贴边软着陆）** / **怪物密度（浅段适应期·深段还原原版·在场峰值封顶）** /
**战斗牌保底与占比** / localStorage 往返。

**转向手感 + 怪物密度 + 抽卡占比**（本文档里所有「旧/新」对照数字的来源）:

```bash
node test/probe-feel.mjs after      # 含同代码 A/B：把键盘参数按旧版等价覆盖，不必回滚文件
```

只跑「局时长带」的快循环（全套 verify 要 3 分钟，这个 40 秒；可传 CFG 覆盖做单旋钮对照）：

```bash
node test/probe-band.mjs
node test/probe-band.mjs '{"mobIntro":1}'      # 例：把刷怪率还原成原版做对照
```

**漏光扫描**：光照半径外做全画布扫描，排除水母/怪物生物光/受损血条/准星光束后，
不许有任何一个像素比黑基准亮。**注意准星与光束必须排除** —— 实测不排除会有 51 个「亮点」误报：

```bash
node test/probe-darkleak.mjs
```

难度曲线与手感标定（压力曲线表 + 60 种子普查 + 压力开关 A/B + 加速响应时间）：

```bash
node test/probe-diff.mjs
```

强化系统与难度再标定（抽卡规则 400 次 + 带/不带强化同种子对拍 + 可复现性）：

```bash
node test/probe-skills.mjs
```

手感探针（鼠标丝滑、空格卡顿那类问题的定位工具，4 组真事件对照）：

```bash
node test/probe-input.mjs
```

战斗探针（0/999/1100/1500m 刷怪普查 + 血量/伤害对拍 + 电球与猎手扣氧 + 四张战斗牌）：

```bash
node test/probe-combat.mjs
```

难度标定（固定种子脚本 AI 对局，看中位时长是否仍在 60~120s 带内）：

```bash
node test/probe-balance.mjs
```

## 调参旋钮

手感与难度**全部集中在 `index.html` 顶部 `CFG`**，改一行即可重调，不用翻实现：

- `o2Drain` `boostDrainMul` —— 生存时长
- `boostRamp` `speedRamp` —— **下潜手感**：踩加速/松开的响应速度（越大越跟手，`boostRamp:24` ≈ 117ms 到 95%）
- `diffHalf` —— **深渊压力的「半程深度」**：调大＝加压更慢，调小＝加压更早。压力 = `d/(d+diffHalf)`
- `drainGain` `bubbleLoss` `hitGain` `rowGain` `gapGain` `extraObst` —— 压力对「耗氧 / 气泡含氧 / 伤害 / 障碍密度 / 走廊宽度 / 障碍个数」各自的增益，**全部是「压力 × 增益」的线性系数**，想更狠就一起往上加
- `hitDamage` `bubbleO2` `bubbleThin` —— 基准碰撞惩罚与补给密度
- `rowNear/rowFar` `gapNear/gapFar` `gapStep` —— 浅水段的障碍密度与走廊宽度（`gapStep` 是「保证过得去」的关键，别调太大）
- `lightNear/lightFar` `jellyLight` `jellyDecay` `jellyCap` —— 视野收缩曲线 + 水母光强/续航（`jellyDecay` 是每秒衰减量，调小＝照得更久）
- `boostMul` `boostSpeed` `baseSpeed` —— 风险收益比
- `lvBase` `lvStep` —— **强化节奏**：`lvBase` 调大＝第一张牌来得更晚，`lvStep` 调大＝后面越来越稀
- `atkDmg` `atkEvery` `atkHalfW` `atkReach` `beamT` —— **普攻**：伤害 / 连发间隔 / 判定半宽 / 射程（光束长度上限）/ 视觉残留
- `rockHP` `cableHP` —— 障碍物血量（石头 / 泥鳅）
- `aimLerp` `aimAutoR` `aimIdle` —— **准星**：跟手速度 / 自动瞄准锁敌半径 / 鼠标停手多久交给自动瞄准
- `mobFrom` `hunterEvery` `spitterEvery` `boltEvery` —— **猎食者**：出场深度与两类怪的刷新间隔（越深自动乘 `0.7+1.4×压力`）
- `hunterHP` `hunterRush` `huntDamage` `spitterHP` `spitterRush` `boltDmg` `boltSpeed` `killScore` `breakScore` —— 怪物血量·速度·伤害·击杀得分
- `SKILLS[]` —— 技能池：加一行＝加一张牌（`minD` 解锁深度、`max` 等级上限、`w` 抽卡权重）；效果接在 `recalcMods()` 的 `G.m` 上

逻辑分辨率 `W0×H0 = 960×600`，等比例居中缩放；逻辑步长固定 `1/120s`，帧率不同手感一致。

## 目录

```
index.html             游戏本体（唯一必需文件）
test/verify.mjs        无头浏览器验收脚本
test/probe-diff.mjs    难度曲线 / 手感 标定脚本
test/probe-skills.mjs  强化系统与难度再标定
test/probe-input.mjs   输入手感探针（键盘/鼠标/自动重复 四组对照）
test/probe-combat.mjs  战斗探针（刷怪深度普查 / 血量伤害对拍 / 怪物还手 / 战斗牌）
test/probe-balance.mjs 难度标定（固定种子对局，中位时长是否仍在 60~120s 带内）
shots/                 验收截图（01 标题 / 02 游玩 / 03 结算 / 04 自检 / 05 深渊 / 06 竖屏 / 07 三选一 / 08 一身强化 / 09 黑暗 / 10 战斗）
```

## 三条路线（第二次强化起）

第二次强化时三张路线卡一起端上来，**选一条，另外两条永久关闭**。选路线只给「技能本身」；下面那些数值要靠之后的路线牌一级一级点出来。不带路线标记的基础牌（收益 / 生存 / 机动 / 采集 / 战斗）任何路线都能抽到。

| 路线 | 主技能 | 专属牌 | 玩起来 |
|---|---|---|---|
| 法师 | Q 冲击波 | 过载 · 凝神（冷却 x0.88） · 震荡（伤害 +40） · 扩散（半径 +45） | 冷却降、伤害升、范围大 |
| 照明使者 | F 探照 | 长明 · 灼光（探照期间每秒灼伤 30） · 回春（每秒回氧 1%） · 光盾（受伤 -35%） | 开灯期间灼伤 + 回氧 + 护盾 |
| 战士 | E 突进 | 余燃 · 铁躯（氧气上限 +22） · 猛撞（撞击 +50，怪也能撞） · 噬血（撞死回氧 2.5%） | 血厚、撞击狠、吸血 |

**技能改用鼠标右键释放** —— 左手要按住 A/D 转向，Q/E/F 按着别扭。**技能不是白送的**：Lv2 的强化三选一里选定一条路线才有技能，右键 / Q E F / 右下角按钮放的都是「你选的那一个」，另外两条永久封闭（没选路线时三个按钮画虚线写「锁」）。
